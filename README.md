## certamen 2 de taller de lenguaje de programacion
se empezo  a ocupar backend con python y framework de Django
## Instalación de Dependencias

Para instalar las dependencias del proyecto, ejecuta el siguiente comando en la línea de comandos:

pip install -r .\requirements.txt
